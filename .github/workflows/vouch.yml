name: Vouch User

on:
  issue_comment:
    types: [created]
  discussion_comment:
    types: [created]

permissions:
  contents: write

jobs:
  vouch:
    runs-on: ubuntu-latest

    steps:
      - name: Check if commenter is authorized
        run: |
          ASSOCIATION="$COMMENT_AUTHOR_ASSOCIATION"
          if [[ "$ASSOCIATION" != "OWNER" && "$ASSOCIATION" != "MEMBER" && "$ASSOCIATION" != "COLLABORATOR" ]]; then
            echo "User is not authorized to vouch (association: $ASSOCIATION)"
            exit 1
          fi
        env:
          COMMENT_AUTHOR_ASSOCIATION: ${{ github.event.comment.author_association }}

      - name: Check if comment contains !vouch
        run: |
          if [[ "$COMMENT_BODY" != *"!vouch"* ]]; then
            exit 1
          fi
        env:
          COMMENT_BODY: ${{ github.event.comment.body }}

      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Get author (issue or discussion)
        id: author
        run: |
          if [ "${{ github.event_name }}" = "discussion_comment" ]; then
            AUTHOR="${{ github.event.discussion.user.login }}"
          else
            AUTHOR="${{ github.event.issue.user.login }}"
          fi

          echo "author=$AUTHOR"
          echo "author=$AUTHOR" >> $GITHUB_OUTPUT

      - name: Add to VOUCHED.md
        run: |
          USER="@${{ steps.author.outputs.author }}"
          FILE=".github/VOUCHED.md"

          if ! grep -q "$USER" $FILE; then
            echo "- $USER" >> $FILE
          fi

      - name: Commit changes
        run: |
          git config user.name "vouch-bot"
          git config user.email "actions@github.com"
          git add .github/VOUCHED.md
          git commit -m "Add vouched user: ${{ steps.author.outputs.author }}" || echo "No changes"
          git push